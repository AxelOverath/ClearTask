<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ClearTask.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ClearTask.Views"
    xmlns:views="clr-namespace:ClearTask.Views.Pc"
    Shell.FlyoutBehavior="Flyout"
    Shell.NavBarIsVisible="False">

    <TabBar Shell.TabBarBackgroundColor="#288d52" Shell.TabBarUnselectedColor="White" Shell.TabBarForegroundColor="Aquamarine" >
    <ShellContent
        ContentTemplate="{DataTemplate local:TaskList}"
        Route="Login" />

    </TabBar>

    <!-- Admin navigatiebalk -->
    <TabBar x:Name="AdminNavBar" IsVisible="False">
        <Tab Title="Gebruikers" Icon="users.png">
            <ShellContent ContentTemplate="{DataTemplate views:UsersPage}" />

        </Tab>
<!--
        <Tab Title="Tags" Icon="tags.png">
            <ShellContent ContentTemplate="{DataTemplate local:TagsPage}" />
        </Tab>

        <Tab Title="Sectoren" Icon="sector.png">
            <ShellContent ContentTemplate="{DataTemplate local:SectorsPage}" />
        </Tab>

        <Tab Title="Taken" Icon="tasks.png">
            <ShellContent ContentTemplate="{DataTemplate local:TasksPage}" />
        </Tab> -->
    </TabBar>

</Shell>

